# OSSM Flagship -- decisions ledger

The reasoning, the measurements, and the option-by-option design record live in
`design-considerations.md` (moved from the Nucleus repo 2026-09-22; its numbers
came from the 2026-09-19/20 bench and their status home is the dev board). This
file is only the ledger of RULINGS, dated, and where a ruling supersedes that
document's "likely" option it says so. Do not restate the record here.

## Fixed facts (from the record)
- Target motor: **YZ 60AIM40F**, 200 W, 7 A. The board is rated to also drive
  the 350 W **60AIM40** at 9.5-10 A: power path **10 A continuous** (section 2).
- Supply: **Mean Well GST360A36**, 36 V; its OVP trips at 37.8-48.6 V and
  latches, which is the real bus ceiling (section 1).
- Regen, measured: powered 6.8 W mean / 141 W peak (4.8 % duty); unpowered
  back-drive 13.6 W mean / 189 W peak; hard throw back-EMF 58.6 V (section 1).
- Shunt clamp threshold ~45 V, below the OVP, which requires the input section
  to block reverse current (section 5). Sizing ~7 W continuous / ~150 W peak
  once the motor switch removes the unpowered case.
- Motor ports: 6-way power+motion (+V, GND, PU+/-, DIR+/- carrying A/B in
  quadrature mode) and a 10-way comms/status block (section 11).

## Rulings
| Date | Ruling | Relation to the record |
|---|---|---|
| 2026-09-22 | Board 58 x 58 mm | new |
| 2026-09-22 | Heatsink **40 x 40** on the BACK, centered; screws in the edge ring | supersedes section 9's "likely" 50 x 50 on a twist-on cap; the record itself notes 40 x 40 as the NEMA 23 common size |
| 2026-09-22 | Through-hole ONLY in the 9 mm edge ring (XT30 and connectors that cannot be SMD); the central 40 x 40 stays free for the sink | consistent with section 11 (XT30 is the one through-hole part that survives the all-SMD rule) |
| 2026-09-22 | **Single-sided assembly, everything on top**; shunt resistors on top over filled+capped thermal via fields; filled+capped vias throughout (via-in-pad allowed) | supersedes section 9's "factory reflows the top, operator hand-solders the back" |
| 2026-09-22 | Regen shunt on-board (two TO-263 over the via field) with the external-resistor connector and the parallel/replacement solder jumper | as section 5's "likely" |
| 2026-09-22 | RS485 via a 3.3 V transceiver (THVD1450/MAX3485 class) from the P4 UART in RS485 half-duplex mode; NOT isolated (the drive's comms side is opto-isolated and 5 V-fed from this board); quadrature via 74AHCT125 | new detail under section 11 |
| 2026-09-22 | XT30 for the 36 V bus; signals in a separate shell | as section 11 |
| 2026-09-22 | Stack-up: 6-layer, mostly pours, valued as heat spreading as much as routing; 4-layer 2 oz remains the alternative until the fab quote and the thermal numbers argue | superseded 2026-09-22 (below) |
| 2026-09-22 | **Fab spec, quoted at JLCPCB:** 6-layer, **1 oz every layer**, TG155, 1.6 mm, white mask / black silk, epoxy filled and capped vias (free at 6 layers), **0.3 mm via drill** throughout, OSP finish with the fab vacuum-packing in groups so unopened boards keep, or ENIG for ~$20 more on the order; finish not yet chosen. 40 pcs quoted at $112 (5 pcs was ~$20 less) | supersedes the 6-layer "leaning" row and the 2 oz outer proposal in `stackup.md`; arithmetic there re-run for 1 oz |
| 2026-09-22 | First batch is hand-soldered by the operator; group-buy boards are assembled from the same 40; **parts purchase on hold**; assembly cost decided when gerbers exist | new |
| 2026-09-22 | **Power connectors: two XT30, right-angle (XT30PW-M), bus IN and bus OUT to the motor**, in the edge ring | as section 11 (XT30 for power); fixes the count at two |
| 2026-09-22 | **Stamp-P4 centered on one edge, USB-C facing out of that edge** ("down" in board view) | new; section 8 names the stamp but not its position |
| 2026-09-22 | **Bulk capacitance symmetric about the stamp's centerline**: SMD aluminum electrolytic (polymer) plus MLCC, mirrored left/right | section 7 detail; electrolytics are SMD cans so the through-hole ruling holds |
| 2026-09-22 | **Signal connectors: JST GH (1.25 mm, latching), right-angle SMD (SMxxB-GHS-TB)**: drive motion pairs, drive comms/RS485, fan, thermistor | section 11's "separate signal connector", now named |
| 2026-09-22 | Stamp-AddOn C6 is a daughterboard stacked on top of the stamp; its antenna sits above the stamp, so the radio sets no board-edge or copper-keepout constraint. Keep tall metal (electrolytic cans) from standing above the add-on's height beside it | resolves the antenna open item |
| 2026-09-22 | Harness: pre-crimped GH leads on the board end, operator crimps the far end | new |
| 2026-09-22 | **Motion is emitted by the P4 (LP-core quadrature); no RP2350 on the board** | supersedes section 8's "plus an RP2350 for motion"; operator dropped it after the P4 LP-core emitter proved out on the bench |
| 2026-09-22 | **Passives: common, slightly larger sizes.** 0805 default, 1206 for power, bulk and high-voltage positions, nothing below 0603. Operator picks connectors; the schematic carries generic connector placeholders until then | new |
| 2026-09-22 | **Input protection latches off on a fault (LTC4364-1), with a red fault LED powered from the raw input and lit by FLT.** A latched fault kills every rail including the MCU's, so the indicator cannot depend on the board | new; section 3 detail |
| 2026-09-22 | **Candidate ICs accepted for detailing:** LTC4364, TPS48111, TLV1805 + TLV431, 3x TPS54560B, INA228, THVD1450, 74AHCT125 | new |
| 2026-09-22 | **Logic power comes from the stamp's own rails**: +5V_SYS = stamp SYS_5V (pad 39, its OR of VIN and USB-C) for the 74AHCT125, the drive's 485_5V and the status LED; +3V3_SYS = stamp SOC_3.3V (pad 28) for the INA228, the RS485 transceiver and every pull-up. A USB-only bench session therefore emits quadrature. The 3.3 V buck becomes accessory-only (+3V3_ACC); the 5 V buck still feeds the stamp's VIN | new; section 10 detail. Also removes pull-ups to a rail that could be dead while the P4 is alive |
| 2026-09-22 | **Rails detailed** per the TPS54560B datasheet design procedure (values on the Rails sheet): 400 kHz, 4 A per rail, VIN 20-48 V, UVLO start 20 V / stop 17 V | section 10 |
| 2026-09-22 | **Schematic drawing rule: wire it, don't label it.** Discrete parts are wired to the IC pins they serve and each section reads as one drawn circuit. Labels only where a net leaves the sheet (power symbols for rails, global/hierarchical labels for signals). A sheet of label-to-label connections is not done | new; applies to every sheet |
| 2026-09-22 | **Input fuse: Nano2 453 15 A very-fast (65 V DC, 150 A interrupting), SOLDERED, first in the path after BUS IN.** Supersedes the same-day OMNI-BLOK socket: the GST360A36 (10 A, hiccup-mode protection) cuts out on a short long before this fuse would blow, so it only ever acts with a stiffer source (bigger supply, battery), and a part that almost never blows gains nothing from a socket that sat at its own 10 A rating. About 3 mm tall. **No polyfuse**: the LTC4364 current limit plus the TPS48111 trip are the resettable protection | new; section 3 |
| 2026-09-22 | **Motor switch detailed** per TPS48111 datasheet Figure 9-14 (values on the sheet): 2 mOhm sense, over-current 15.2 A / 10 ms, short-circuit 30 A / 1.2 us, **latch-off** on over-current (firmware re-arms by toggling MOTOR_EN; veto-able: auto-retry is one resistor removed), pre-charge 100 R via Q403 and Q402's body diode, UVLO ~23 V, MMBT3904 over-temperature beside the FETs, SMBJ48CA on the output. INA_ALERT_N now pulls down a bus-powered EN divider, so an unpowered INA defaults the switch off | section 4 |
| 2026-09-22 | **Regen shunt detailed**: TLV1805 + TLV431 self-acting clamp, ON 44.7 V / OFF 41.7 V (348k / 10k / 1.40M), 1 nF divider filter, 47 R gate drive, the 10k + 3.3k CLAMP_MON divider doubles as the gate pull-down (clamp off whenever +12V is down), SHDN tied low (always armed), SMBJ48A across the switch | section 5 |
| 2026-09-22 | **Connectors are placed by the operator during layout.** Schematic sheets end every off-board signal in a named net (global label); existing generic connector placeholders are disposable | new |
| 2026-09-22 | **Drive IO detailed**: THVD1450 (DE and /RE tied, 120 R termination DNP), SM712 RS485 ESD at the cable end, KiCad 74AHCT125 per-gate symbol (gates 1-2 drive DRV_PU+ / DRV_DIR+, gates 3-4 disabled), status inputs 10k pull-up to +3V3_SYS + 1k series into the P4 | sections 11-12 |
| 2026-09-22 | **Sensing detailed**: INA228 at 0x40 on the Kelvin taps, ALERT shared with the motor-switch EN; I2C pull-ups fitted only if the stamp/add-on lacks them; thermistor 10k pull-up + 1k/100n into ADC1; fan supply by solder jumper (12 V or 5 V), tach 10k pull-up + 1k series, **PWM open-drain through a 2N7002** because a fan pulls its PWM input to 5 V and the P4 is not 5 V tolerant (firmware inverts) | sections 10, 12 |
| 2026-09-22 | **Input protection detailed** per LTC4364 datasheet Figure 1: 2.5 mOhm sense (20 A, 10 A folded back into a short), 22 nF HGATE (~40 ms ramp, ~0.4 A inrush, timer idle at start), 100 nF timer (~1 ms in a dead short), **output clamp 51.5 V (402k/10k), deliberately above the 44.7 V regen clamp so regen is never read as a surge by the latching -1**, UV 20 V / OV 50 V (374k/15k/10k), 15 V gate zeners (datasheet, >= 24 V), SMBJ45CA bidirectional input TVS, fault LED resistor 27k (FLT sinks <= 2 mA) | section 3 |
| 2026-09-22 | **Power FETs: Nexperia PSMN3R7-100BSE (100 V, 3.95 mOhm, D2PAK, enhanced linear-mode SOA) in all five power positions** (input Q1/Q2, motor switch Q401/Q402, regen clamp Q501): one BOM line. SOA ~30 A at 36 V for 1 ms (25 C) vs the 10 A folded-back short on Q1. Pre-charge Q403: Diodes DMN10H220L (100 V, SOT-23). Motor-switch bootstrap raised to 1 uF for 2 x 176 nC gate charge | resolves the FET open items |
| 2026-09-22 | **Regen resistors: 3 x Vishay D2TO35M 24 R = 8 R** (operator: 2-3 to spread the load). Multi-pulse rated, element isolated from the tab (2 kV), tabs tied to GND and stitched into the back sink. ~2.3 W each continuous, ~47 W each at the 141 W powered peak | resolves the TO-263 resistor and pulse-energy open items; Q501's own tab is the switch node, not ground |
| 2026-09-22 | **Fan supply jumper JP2 ships bridged to 12 V** (KiCad SolderJumper_3_Bridged12: a copper trace joins pads 1-2); 5 V fans: cut the 1-2 trace, bridge 2-3 with solder | operator; section 10 |
| 2026-09-22 | **Kelvin shunts: Vishay WSK2512 4-terminal** (Bourns CSS2H is 2-terminal) on the T2.21 mm footprint that WSK2512 uses at 1-4.9 mOhm: INA228 R2 1 mOhm (WSK25121L000FEA), motor switch R401 2 mOhm (WSK25122L000FEA), LTC4364 R1 2.5 mOhm (WSK25122L500FEA) now 4-terminal with SENSE and OUT on the taps (OUT is a sense-only pin) | operator ask; sections 3, 4, 12 |
| 2026-09-22 | **Bulk: 2 x 100 uF 63 V hybrid polymer (Panasonic EEH-ZA1J101P, 10 x 10.2 mm) mirrored about the stamp, + 2 x 2.2 uF 100 V X7R 1210 (Murata GRM32ER72A225KA35L)**; the same MLCC is every buck's input cap. 63 V covers the 51.5 V LTC4364 output clamp; ESR damps the ceramics as section 6 intends | section 6 |
| 2026-09-22 | Regen resistor ordering: Vishay D2TO35M, 24 R +-2 % (SAP pattern D2TO035M + 24R00 + G + packaging + E3; D2TO35M range is 10 R-10 k) | refines the regen resistor ruling |
| 2026-09-22 | **Regen clamp revised** (compared against the OSSM alt board's brake chopper): hysteresis 1.4 V, ON 44.5 V / OFF 43.2 V (R503 3.01M); UCC27517 low-side driver + 10 R gate (comparator-driven edges were ~1-2 us, watts in Q501); D503 SS310 (LCSC C15874) freewheel across the resistor bank so trace and J3 lead inductance clamps at the bus; D502 amber LED on the gate; CLAMP_MON goes to a P4 capture pin for on-time counting (power = duty x V^2 / 8 R, published over Valence); one NTC (TH501, SHUNT_TEMP) beside the hottest-placed D2TO35M | supersedes the regen-detailed row's thresholds, gate resistor and monitoring |
| 2026-09-23 | **Regen gate driver removed**: U3 (TLV1805 push-pull, 0.3 us into 4 nF per its datasheet, ~100 mA) drives Q501 directly through R505 10 R. Q_gd <= 77 nC gives ~0.7 us edges, ~0.16 W switching + ~0.1 W conduction at the worst-case ~2.8 kHz clamp cycle. The prior row's "1-2 us, watts in Q501" was wrong. Slow edges also keep L di/dt small (~1 V on board trace), so no snubber pads. D503 stays for the J3 flying leads | corrects the regen-clamp-revised row; the UCC27517 and C503 are out |
| 2026-09-23 | **Shunts moved to LCSC stock** (operator): INA228 R2 = Vishay WSK12161L000FEA 1 mOhm (LCSC C844476), true 4-terminal, LCSC footprint checked against Vishay's land pattern. LTC4364 R1 and motor switch R401 = Bourns CSS2H-2512K-2L00F 2 mOhm 5 W (LCSC C2093761, one BOM line), 2-terminal on `flagship:R_2512_Shunt_PseudoKelvin`: Bourns land, sense pads 2/3 at the inner pad edges, net-tied (DRC-clean). R1 at 2 mOhm moves the LTC limit to 25 A / 12.5 A folded back (was 20 / 10); Q1 (input hot-swap FET) short-circuit margin inside its ~1,080 W / 1 ms / 25 C SOA: ~2.4x at typical foldback, ~1.9x at the 32 mV max foldback (16 A, ~580 W; was ~2.3x worst case at 2.5 mOhm). 2.5 mOhm is not in the CSS2H range; 2.3 mOhm is not stocked at LCSC or JLCPCB (searched 2026-09-23), so 2 mOhm stands (operator: no bespoke parts) | supersedes the WSK2512 Kelvin row |
| 2026-09-23 | **Bulk: 4 x Panasonic EEH-ZA1J101P** (operator; was 2). 63 V hybrid polymer is 10.2 mm tall from 33 to 100 uF, so smaller cans buy no height; only 22 uF (6.3 x 7.7) is shorter. C5/C6 added on +BUS beside C1-C4. Inrush on the 40 ms HGATE ramp rises to ~0.6 A into ~670 uF, still far below the 25 A limit | supersedes the bulk count in the Kelvin/bulk row |
| 2026-09-23 | **Drive connectors (operator), mirrored across the board**: one side carries the motor XT30 (bus out through the motor switch) beside a **4-pin GH MOTION**: 1 QUAD_A, 2 GND, 3 QUAD_B, 4 GND (to the drive's PU+ / PU- / DIR+ / DIR-). The mirrored side carries a **third XT30 for the external regen resistor** beside a **6-pin GH COMMS**: 1 +5V_SYS (the drive's 485_5V), 2 GND (COM), 3 485A, 4 485B, 5 WR (alarm), 6 RDY. **ZO (encoder index) is not brought out**: absolute position comes over Modbus, and it keeps the GH count at 4 + 6. Its pull-up and series resistor are deleted from Drive IO | supersedes "two XT30"; resolves the external-regen-connector open item; YZ 57AIM manual p2-3 |
| 2026-09-23 | **Two buttons, no reset button** (operator): SW1 HOME (press = home, hold = reset, acted on release) and SW2 PAIR (hold = pair, held at power-on = AP config mode, press TBD), both on ordinary GPIOs with the P4's internal pull-ups. **Never on a strap pin**: G35 held low at power-on is ROM download mode, so a button there could never reach firmware. CHIP_EN and G35 go to tweezer pads TP901 / TP903 beside GND pad TP902 for rescue | the P4 strap table; firmware behavior is Nucleus's |
| 2026-09-23 | **Status LED data through 74AHCT125 gate 3** (operator): the WS2812B's input-high threshold is 0.7 x VDD = 3.5 V on +5V_SYS, above the 3.3 V GPIO (guaranteed VOH ~2.6 V). LED_DATA -> gate 3 -> LED_DIN, R601 10k pull-down holds it low while the P4 boots. No new part | new |
| 2026-09-23 | **One schematic sheet, drawn as wires** (operator): A1, input on the left, the Stamp-P4 in the middle, outputs on the right, each block in a dashed section box, all section notes in one NOTES panel. Every signal between blocks is a drawn wire; GND and the low-voltage rails stay power symbols/labels; the 36 V bus is one wire from the input to the motor switch and over the top to the regen clamp. The seven hierarchical sub-sheets are retired. Supersedes the per-sheet "wire it, don't label it" rule, which now applies to the whole sheet | operator; netlist verified identical to the hierarchical design except the rows below |
| 2026-09-23 | **Provisional P4 pin map** (drawn so wires don't cross; swap freely at layout, one re-wire each). Right: G23 INA_ALERT_N, G22 PRECHARGE_EN, G21 MOTOR_EN, G20 MSW_FLT_N, G19 MSW_IMON, G18 SHUNT_TEMP, G17 CLAMP_MON, G16 THERM (analog on ADC1), LPG13 FAN_PWM, LPG6 FAN_TACH, LPG3 QUAD_A, LPG5 QUAD_B, LPG2 LED_DATA, G32 RS485_RX, G41 RS485_DE, G33 RS485_TX, G31 DRV_ALM, G28 DRV_RDY, G35 (pad 36) BOOT pad. Left: LPG11 I2C_SCL, LPG9 I2C_SDA (swapped from the earlier note so the two wires don't cross), G49 BTN_HOME, LPG4 BTN_PAIR, CHIP_EN EN pad. Pad 48 (G35's second pad, same GPIO inside the module) is no-connect. Straps avoided. Fan GH4 = 1 GND, 2 FAN_V, 3 TACH, 4 PWM (PC fan order); thermistor GH2 = 1 THERM, 2 GND (both veto-able) | open item: LPG0-15 GPIO map before committing A/B pins |
| 2026-09-23 | **Firmware probe header J8** (operator: something to hook logic-analyzer / scope probes onto for firmware testing): 1x4 2.54 mm through-hole in the edge ring, 1 QUAD_A, 2 GND, 3 DBG, 4 QUAD_B, tapped on the P4 side of the 74AHCT125 (the 3.3 V signals firmware emits). DBG = spare P4 G50 through R602 1k, a firmware trigger/marker pin. Fit the header only on test boards | new |
| 2026-09-23 | **Power FETs: Infineon IPB042N10N3G in all five positions** (100 V, 4.2 mOhm max, D2PAK on the same TO-263-2 footprint; LCSC C69300, ~21.6k stock, ~$0.48 @100; DigiKey IPB042N10N3GATMA1 second source). Replaces PSMN3R7-100BSE ($9.77 and out of stock at LCSC, backordered at DigiKey, ~$48/board). **C802 LTC4364 timer 100 nF -> 22 nF C0G** so Q1 holds SOA: timer current is ~129 uA with 36 V across Q1, fault turn-off at 1.35 V, so ~0.23 ms in a dead short (was ~1.05 ms). Worst case is a heavy fault with OUT just above 2.5 V (full 25-27.5 A limit at ~33 V, ~920 W for ~0.25 ms): ~1.75x inside the 25 C SOA, ~1.35x at a 60 C case, the same margin the PSMN part had at 1 ms. Start-up inrush (~0.4 A) never runs the timer. For Q2/Q401/Q402/Q501 only, CR Micro CRSS042N10N (LCSC C410926) is a drop-in second source; not for Q1 (its SOA curve looks computed) | supersedes the PSMN3R7-100BSE row; SOA read from the Infineon datasheet graph, +/-10 % |
| 2026-09-23 | **Bulk: 4 x Panasonic EEH-ZA1J820P, 82 uF 63 V hybrid polymer, 10 x 10.2 mm** (operator). EEH-ZA1J101P does not exist: the ZA series tops out at 82 uF at 63 V. ~330 uF on board, ~480 uF with the drive's 150 uF; inrush ~0.4 A. Cheap machine-placeable fallback: Honor RVT1J101M1010 100 uF 63 V electrolytic (LCSC C28241) | supersedes the 4 x EEH-ZA1J101P row |
| 2026-09-23 | **Build and purchasing split** (operator): JLCPCB machine-places the cheap, fiddly parts (resistors, ceramics, small passives); pricey chips, modules and power parts are hand-soldered from DigiKey stock, so a board that turns out not to work does not carry hundreds of dollars of silicon | refines the first-batch ruling |
| 2026-09-23 | **Bus-powered overvoltage trip on the motor switch** (operator: whatever the cause, shunt open, clamp FET failed, anything, above ~51 V the motor is cut): +BUS -> D402 BZT52C51 -> R413 10k -> Q405 MMBT3904 (R414 10k base pull-down), collector on the TPS48111 EN/UVLO node, the same wired-OR node the INA228 ALERT already pulls. Trips at ~49-55 V (zener tolerance), below the TPS54560B's 60 V input, above the 44.5 V regen clamp; may also trip during an input surge (LTC4364 output clamp 51.5 V), the safe direction. Needs no firmware; self-re-arms when the bus falls (a failed-open bank gives a safe hiccup). The PSU's own OVP never sees regen: Q2 blocks reverse current, so without this trip a failed clamp would leave the bus an island. The TPS48111 has no OV input (only the TPS48110, which lacks the pre-charge driver). Firmware adds the INA228 bus-overvoltage alert (~50 V, latched) as the sharper second layer, and reads the EN node on ADC pin G23 (running ~1.8 V, tripped ~0 V) | new |
| 2026-09-23 | **Supported input: 24-36 V** (operator: some builds run a lower-power motor from 24 V). Two resistor changes, nothing else in hardware: **R408 24.9k -> 30.1k** moves the motor-switch UVLO from ~23.5/22.1 V to 19.6/18.4 V (on/off; the EN node then reads 3.31 V at 55 V, still safe for P4 G23); **R803 15k -> 17.8k** moves the LTC4364 UV from 20 V to 18.1 V (OV 49.9 -> 50.2 V). Brown-out order is motor switch (18.4 V), then input switch (18.1 V), then bucks (17 V). Unchanged and fine at 24 V: bucks (20-48 V design), regen clamp 44.5 V (protects the drive's maximum, not tied to the supply), OVP trip, INA228, gate drives, 63 V caps. Costs: on 36 V the motor stays connected ~4 V deeper into a brown-out; firmware rescales the G23 reading. **Motor pairing:** 24 V builds are expected to be the 57AIM30, a few the 60AIM40F (200 W ~ 8.3 A at 24 V, inside the 10 A path); the 350 W 60AIM40 is a 36 V motor (~14.6 A at 24 V would trip the 15.2 A motor switch). Firmware: pick a 24 V or 36 V profile from the bus reading at boot (INA228 under/overvoltage alerts, reporting) | new; supersedes the 20 V UV and ~23 V motor UVLO in the input/motor rows |
| 2026-09-23 | **Heatsink: common 40 x 40 x 6 mm copper sink on its pre-applied thermal adhesive tape**, floating (the tape is the electrical insulator; no screws needed). Requires a dead-flat, fully solder-masked bottom under the sink: nothing assembled on the bottom (single-sided stands), filled+capped vias. Hot parts stay on top: FET tabs and each regen-resistor pad sit on per-net copper islands stitched by via fields to matching bottom islands under the sink. Cooling model (operator): the load is intermittent, the board mounts vertically so natural convection works, and a machine hot enough to matter is already running a fan for its motor, which also cools the board | supersedes the edge-ring-screws detail of the heatsink row; closes the 'sink grounded or floating' and 'TIM and heatsink part choice' open items |
| 2026-09-23 | **Regen bank: 24 x UniOhm AS122WJ0201T4E, 200 R anti-surge 2512 in parallel = 8.33 R** (LCSC C2770484, ~$0.08 each; operator). Replaces 3 x D2TO35M 24R, which no distributor stocked. 2 W each at 70 C (48 W bank); at the measured regen: powered 6.8 W mean = 0.28 W each, unpowered 13.6 W mean = 0.57 W each, 189 W peak = 7.9 W each for the pulse (the anti-surge rating is what carries it). Each chip bridges a +BUS island and a switch-node island, both stitched by filled vias to bottom islands under the heatsink tape; no tabs, so no GND connection | supersedes the D2TO35M rows |
| 2026-09-23 | **Fan: 3-pin (1 GND, 2 +V, 3 TACH), 5 V default, high-side PWM** (operator: 3-pin 5 V fans are common, keep the tach). JP2 now ships bridged to **5 V** (pads 1-2), 12 V by cutting 1-2 and bridging 2-3. Q702 AO3401A P-FET on the fan's +V (LCSC C15127), R702 1k gate pull-up (fan off by default), Q701 2N7002 pulls the gate low through R703 470R (P4 high = fan on, no inversion); the 1k/470R divider holds Vgs at -8.2 V on the 12 V setting (AO3401A rating +-12 V) and -3.4 V on 5 V, with ~1 us gate edges, D701 B5819W flyback (LCSC C8598). Switching the high side keeps the fan's ground solid, so the open-collector tach stays valid; tach pulses exist only while the fan is powered, so firmware reads it during the on-time or with a brief full-on pulse. J6 is a 3-pin GH; a 2-pin fan uses pins 1-2, a 4-pin fan plugs in without its PWM | supersedes the JP2-12 V row and the fan half of the sensing row |
| 2026-09-23 | **Fan supply filtered to DC** (operator: PWM for DC control): L701 Sunlord SWPA6045S101MT 100 uH shielded (Isat 0.95 A, LCSC C60633) + C701 22 uF 25 V X5R (C12891) after Q702, D701 as the freewheel diode: a controller-less buck at ~50 kHz, so the tach is valid at every speed; the inductor runs discontinuous at fan current, so firmware closes the loop on the tach. **Damping leg R704 1 R + C702 100 uF 25 V (Lelon RVT1E101M0607, C72477)** across C701: simulated hard 0-to-100 % duty step overshoot 0-6 %, was +60-75 % bare (21 V on the 12 V setting). **F701 0.5 A polyfuse (Littelfuse 1206L050/33NR, C19193043)** in the fan feed; a dead header short still sags the selected rail until it trips (~0.1 s), which can reset the P4 on the 5 V setting (eFuse upgrade offered, not taken) | refines the fan row |
| 2026-09-23 | **Connector short protection** (operator: DIY users will short things; polyfuses on power, but a polyfuse cannot protect a logic pin, so series resistors there): F601 0.1 A polyfuse (Littelfuse 1206L010/60NRL, C18198325) on the COMMS 5 V out, whose 1.6 R holds a dead short near 3 A, under the 5 V buck limit, so the board stays up; R603/R604 180 R on the MOTION pulse outputs cap a short at ~25 mA (74AHCT125 abs max) while delivering at least the opto current a 3.3 V S3 GPIO did (the drive runs fine on that; a TXS0108E, a few-kOhm source, did not); R605/R606 1k on the probe-header taps. Already limited: thermistor, tach, WR/RDY (pull-up + 1k), RS485 (transceiver rating + SM712). Excluded: input, motor and external-shunt XT30s (power paths behind the fuse and hot-swap). Bench owed: scope PU edges at full pulse rate | new |
| 2026-09-23 | **External wired E-stop** (operator): J9 GH3 carries COM / NC / NO of a latching button (COM = GND; R901/R902 2.2k pull-ups, ~1.5 mA wetting; C901/C902 100 nF; D901 PESD3V3S2UT, LCSC C84276). **Hardware stop**: NC open (pressed or unplugged) drives Q901 2N7002 through R905 100k and pulls the motor-switch EN low, the same wired-OR node as the bus OVP trip and the INA228 ALERT; no firmware in the path. Firmware reads NC on G39 and NO on G30 through R903/R904 1k: 0/1 normal, 1/0 pressed, 1/1 unplugged ("no E-stop found" warning), 0/0 wiring fault. **Bypass** (G29, R906 100k pull-down) drives Q903 in series with Q902, which conducts only while NO is open, so it can mask ONLY an unplugged cable, never a connected button; every reset clears it (single-session convenience for testing or builds without a button). Releasing the button restores motor power: firmware must not resume motion by itself | resolves the E-stop open item |
| 2026-09-23 | **Budget-build bypasses** (operator: open-source builders may skip the pricier protection): normally-open 15 A wire-link pads (a 0 R chip is rated 2-3 A; the link ships open so fitting both the protection and the link is not a silent bypass) across (1) the LTC4364 input protection (~$12/board: loses reverse polarity, OV/UV, inrush limit and reverse-current blocking, so regen back-feeds the PSU) and (2) the TPS48111 motor switch (~$4: loses the hardware E-stop, the 51 V OVP cut and the INA ALERT cut; E-stop becomes firmware-only). INA228 stays mandatory (sensorless homing). Not yet drawn; the budget-build warnings land in the documentation phase | new |
| 2026-09-23 | **LP pins carry quadrature and nothing else** (operator, non-negotiable): LPG0-15 are the LP core's; only QUAD_A/QUAD_B may use them. Every other signal on an LPG pin moves to an HP GPIO (INA228 I2C on LPG9/11, FAN_PWM LPG13, FAN_TACH LPG6, LED_DATA LPG2, PAIR button LPG4); the INA228 then gets its own I2C bus, which also retires the shared-bus question. LPGn = LP_IO n = GPIOn is verified on silicon: the LP emitter drove LP_IO 15/12 and the scope read them on the stamp's LPG15/LPG12 holes (val-091.4, 2026-09-21). JLC plated via wall = 18 um average (capabilities page), stackup.md corrected. The drive's 4.1-4.5 A limit does not gate the PCB (rated 12.5 A); it moves to the firmware/bench list (Modbus current parameters) | closes three open items; pin moves pending the stamp strap/internal-use check |
| 2026-09-23 | **P4 pin map after the LP-quadrature-only ruling** (supersedes the provisional map's LP entries; GPIO matrix, so any HP pin serves I2C/UART/LEDC/RMT/PCNT; only ADC is pin-fixed): LPG3 QUAD_A, LPG5 QUAD_B, all other LPG pins unused. INA228 SDA G34, SCL G36; LED_DATA G26; FAN_PWM G27; FAN_TACH G38; BTN_PAIR G52. Strap safety from the Stamp-P4 schematic and ESP32-P4 DS v0.7 / TRM ch. 12: G34 is ignored at default eFuses; G36 carries the stamp's 10k pull-up plus the 4.7k I2C pull-up (high, the stamp's own default) and only selects between download modes; G38 is sampled only in download mode; G26/G27 are USB 1.1 OTG pins with no pulls while USB is off (DS: P2, free use). G35 (BOOT) and G37 (ROM log at every reset) stay unused. R24/R25 always fitted: the stamp has no I2C pull-ups, and the C6 add-on does not use I2C. G31/G32 confirmed plain on the stamp (the 'internal I2C on 31/32' claim is a Tab5 pin file copied into the Stamp-P4 Arduino variant) | supersedes the provisional map's LP pins |
| 2026-09-23 | **Budget-build links drawn**: JP801 VIN_RAW -> R1/R2 node (bypasses U1, Q1, Q2, R1; the fuse and the INA228 shunt stay), JP401 +BUS -> MOTOR_V+ (bypasses R401, Q401, Q402, pre-charge). Footprint flagship:Link_Wire_15A: two 3.5 x 4.5 mm SMD pads 7 mm apart for a soldered 14 AWG wire; drawn DNP and out of the BOM so they ship open | implements the budget-build ruling |
| 2026-09-23 | **LP rule restated** (operator): the LP core drives quadrature (LPG3/LPG5) and nothing else; board functions stay on HP pins; the spare LP pads carry OPTIONAL accessory I/O driven by HP peripherals through the GPIO matrix. Mechanism: per-pad LP/HP select is set once at boot, the emitter (lp_quad.c) issues only atomic out_w1ts/w1tc stores on its own two bits, and HP matrix peripherals put no traffic on the LP bus. Firmware rule: HP peripherals only on those pads, never the LP UART or LP I2C. Unmeasured: Nucleus val-091.18 reruns the val-091.4 scope capture with every accessory pad hammered. Accessories are optional; if they ever disturb quadrature, they lose | amends the LP-quadrature-only row |
| 2026-09-23 | **Accessory breakouts** (operator: simple PWM/on-off loads connect to the board; anything with its own logic is a separate Valence device; the target is ONE 0.5 A peristaltic lube pump, which people will add because the board makes it easy). PUMP: 12 V through a Littelfuse 1812L125/33GR (1.25 A hold, 2.5 A trip, LCSC C19078726) + PWM (LPG13, 220 R, 100k pull-down) for an off-board MOSFET trigger module (one-way; the IRF520 module is not 3.3 V-drivable); the polyfuse trips on the channel's own overload far below the 12 V buck's limit. EXT PWM (LPG2, 220 R, 100k pull-down, GH 2-pin, signal only) for an externally powered bigger pump. BUS OUT: +BUS through a Littelfuse 2920L200/60GR (2 A, 60 V, C49326266) for a user's own buck; it is the raw bus, 24-36 V nominal, up to 44.5 V under regen and ~51 V before the OVP trip, so only 60 V-rated bucks (LM2596HV, XL7015, MP9486 class; NOT LM2596/XL4015). BUS OUT (XH) and EXT PWM (GH) sit on different connector families so they cannot be swapped. NEOPIXEL OUT chained after the status pixel's DOUT (100 R), 5 V from the 5 V buck through a 1 A polyfuse (1206L100/24NR, C7542957). BUTTONS: HOME/PAIR in parallel with SW1/SW2 through 1k, plus AUX1/AUX2 (LPG4/LPG6); 10k pull-ups on all four, PESD3V3S2UT ESD. QWIIC/STEMMA QT on its own I2C bus (LPG11 SDA / LPG9 SCL, 2.2k): the INA228 bus stays private (0x40 clashes, and a hung accessory must not stall sensorless homing). GPIO 2x6: 3V3, 5V, 2 GND, HP-UART TX/RX (LPG14/LPG15), IO1-6 (LPG12/10/8/7/1/0), 220 R each. Accessory 3V3 through a 0.2 A polyfuse (1206L020/30NR, C7542932). The stamp's camera connector is never used (operator), so LPG9/LPG11 are free; the C6 SDIO (G40, G42-G48, G51) is on no stamp pad. **Fail loud** (operator): the regen clamp stays on +12V; if an accessory ever drags 12 V down during regen, the clamp browns out and the 51 V OVP trip cuts motor power, which is preferred to a self-healing stumble that wears parts. No bulk caps added to the rails (a cap cannot bridge a ms-scale brownout; the rails are independent bucks off the bus) | replaces the single 12 V accessory output |
| 2026-09-23 | **Input floor stays 24 V** (operator: below 24 V is too lenient; the drive is rated 24-36 VDC). USB-C PD 20 V is NOT supported (the buck UVLO starts at 20 V and the motor switch turns on at 19.6 V, by design). USB-C PD **EPR 28 V / 140 W** through a trigger-to-XT30 cable IS inside 24-36 V and works unmodified, with two conditions: a firmware power profile keeping acceleration peaks under the source's 5 A (PD sources shut off rather than sag), and the LTC4364 stage FITTED (its ideal diode keeps regen out of the charger; never pair a USB-C source with the JP801 budget link) | confirms the 24-36 V row |
| 2026-09-23 | **Board monitor + boot self-check** (operator: the P4 must prove the rails, the INA228, the input stage and the motor switch before motor power, and report any failure; the monitor should watch continuously so the P4 does less). U12 WCH CH32V003F4U6 (QFN-20 3x3, LCSC C5299908, ~$0.29) on the INA228's private I2C bus, powered from +3V3_SYS. ADC channels: VIN_RAW and MOTOR_V+ (160k/10k), +12V (39k/10k), +5V, +3V3_ACC and +5V_SYS (10k/10k), CLAMP_MON and SHUNT_TEMP (1k taps), 100 nF each. FAULT_N (open-drain, 1k) joins the motor-switch EN wired-OR: it can cut motor power, never enable it. **Regen trim (Version C)**: CLAMP_TRIM PWM -> 10k -> 1 uF -> 47k into U3 +IN, 10k pull-down at the pin, R502 10k -> 12.1k: duty 0 = the 44.5 V hardware ceiling, CH32 in reset = 43.5 V, duty 100 % = 24.5 V; firmware sets VIN_RAW + 3 V (+1.2 V on a JP801 budget build, which the trim makes viable) and can only lower the threshold; the clamp stays self-acting with no firmware. The P4 programs the CH32 over SWIO from G50 (image in the Nucleus OTA; blank chips flash on first boot); rescue pads TP1101-1103 for a WCH-LinkE. DBG moved to G37 (the ROM boot log shows on J8.3). A dead or blank monitor blocks motor enable ('monitor missing'). Firmware: Nucleus val-091.19 (monitor), val-091.20 (SWIO programmer), val-091.21 (self-check sequence) | new; amends the regen clamp and pin map rows |

## Open (from the record's section 13, plus today)
- U7 accessory buck: stay 3.3 V (+3V3_ACC) or become +5V_ACC (twin of U6) with an LDO for accessory 3.3 V, so an accessory 5 V short cannot reset the P4 (operator to rule).
- Accessory connectors are placeholders (JST XH for pumps and Neopixel, GH for buttons, SH for Qwiic, 2.54 mm 2x6): operator picks later, leaning XH.
